I planing to build a question bank site using next js

Users can download documents without any auth

Admin login. Secure. 
Admin has previlage to add teachers using their mail(get notified the user with link to login).
Admin can upload PDFs in bulk through a format.

Teachers can upload pdf 
with some fields. i will give you some uis for reference.
reference/image.png is for public users filter button and options.
reference/image copy.png is for the home page.
reference/image copy 2.png is for the admin login page.
reference/image copy 3.png, reference/imagse copy 4.png is qn bank fields for uplods add nessery handls and optimise uploadings .
make the qn number unique.
department sitewide(when one user creates one department then that show for all users also update in filters.).
make subjuct code also unique. when user write a suject code and seved it then the same subject code is typed by another user the deprtment, subject name will be auto select.
do the optposite can select the subject name to code.
need to rename all qn paper to a best name subject + year + qn number add QNHUB at the end..
make bulk upload button only for admin.
in bulk upload make a zip formate then that needs a csv file inside for refernce i will add a folder to understood the structure reference/3 FYUGP QP NOV 25. from that generate the same qn papers incuding intexing also departement, subject , subject code , subject type(minor major creation as site wide).

also make the the home search more relable support year departement, subject, subject code, subject type(minor major).

sync with direct SEO.


fore db use firebase i will share with you my firebase config reference/firebase.js.

make this project with serity first design. fully dynamic.

use rect best practices skils, db optimisation, performance, security. 

for storage buket use https://console.firebase.google.com/u/0/project/sample-firebase-ai-3e3e5/storage/sample-firebase-ai-3e3e5.firebasestorage.app/files


Seo for each document they upload like vlogs sites  if students search for any qn paper on Google it shows the direct link to that document page